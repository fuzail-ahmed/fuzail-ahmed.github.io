# ----------------------------------------------------------------------
# 1. Hugo Build Artifacts
# ----------------------------------------------------------------------

# The folder where Hugo generates the final static site files.
# This folder is created and handled by GitHub Actions, so we never commit it.
public/

# The cache directory generated by Hugo during the build process.
# This is handled and cached by the GitHub Actions 'Cache Go/Hugo Modules' step.
.hugo_build.lock
.sass-cache/
resources/_gen/
.cache/
tmp/

# ----------------------------------------------------------------------
# 2. Local Development Files
# ----------------------------------------------------------------------

# Temporary file used by 'hugo server'
.DS_Store

# IDE and Editor files (common ones)
*.sublime-project
*.sublime-workspace
.vscode/

# ----------------------------------------------------------------------
# 3. Theme/Module Specific (if not using git submodules)
# ----------------------------------------------------------------------

# If you downloaded the theme directly instead of using a submodule,
# you might want to exclude certain theme folders (like build/dist files)
# that the theme generates, but usually, if the theme is clean, this is optional.

# ----------------------------------------------------------------------
# 4. Node/NPM Dependencies (If you run 'npm install' locally)
# ----------------------------------------------------------------------

# You confirmed running Node dependencies in your GitHub Action,
# so if you run npm locally, these should be excluded.
node_modules/

# Log files created during local development
npm-debug.log
yarn-error.log


# Hugo build output
public/
resources/
.hugo_build.lock

# OS & editor
.DS_Store
.idea/
.vscode/
